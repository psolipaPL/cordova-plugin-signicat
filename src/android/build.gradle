repositories {
    flatDir {
        dirs "${projectDir}/libs"
    }
}

dependencies {
   implementation(name:'android-sdk-1.1.5', ext:'aar')
}

android {
    defaultConfig {
        manifestPlaceholders = [
            appAuthRedirectScheme: "https",
            appAuthRedirectHost: "salland-dev.outsystems.app"
        ]
    }
}

